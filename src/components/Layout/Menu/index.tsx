import React from 'react';
import "./styles.scss";

const Menu: React.FC = () => {

    return (
        <div className="menu">
            <button onClick={() => { }} className="menu-button">⋮</button>
        </div>
    );
};

export default Menu;
